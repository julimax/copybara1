name: Echo cada 5 min

on:
  schedule:
    - cron: "*/5 * * * *"  
  workflow_dispatch:        

concurrency:
  group: echo-5min
  cancel-in-progress: true   

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Echo con timestamps
        run: |
          echo "Ping cada 5 min ✅"
          echo "UTC: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          echo "Buenos Aires: $(TZ=Am
