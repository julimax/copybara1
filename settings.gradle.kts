// settings.gradle.kts (raíz del proyecto)
// Note: Removed Develocity plugin to avoid conflict with Gradle Enterprise --scan
// The --scan flag will automatically apply the Gradle Enterprise plugin
