project:
  name: copybara
  version: 1.0.6
  description: Copybara Kotlin Hello World Application
  authors:
    - Juli Gonzalez
  license: Apache-2.0

# Disable signing and deployment - handled by Gradle
signing:
  active: NEVER

deploy:
  active: NEVER

# Only handle GitHub releases
release:
  github:
    enabled: true
    owner: julimax
    name: copybara1
