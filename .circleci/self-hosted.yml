version: 2.1

workflows:
  build-workflow:
    jobs:
      - runner
jobs:
  runner:
    machine: true
    resource_class: resource_class: org-test-copybara/test
    steps:
      - run: echo "Hi I'm on Runners!"
