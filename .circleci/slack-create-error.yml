# .circleci/config.yml
version: 2.1

jobs:
  trigger-b:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          name: Activate pipeline b
          command: |
            curl -X POST \
              -u "${CIRCLE_TOKEN}:" \
              -H "Content-Type: application/json" \
              -d '{
                "branch": "main",
                "parameters": {
                  "objetivo": "imprimir",
                  "mensaje": "Hola Juli desde el pipeline A 🚀"
                }
              }' \
              "https://circleci.com/api/v2/project/gh/julimax/copybara1/pipeline"

workflows:
  trigger:
    jobs:
      - trigger-b
